
https的过程：
1. 浏览器让服务器发送https请求，并带上浏览器所支持的加密算法等信息。
2. 服务器接送到信息之后，将CA证书，证书中包含公钥、加密算法、数字签名、内容指纹等信息发送给浏览器。
3. 浏览器接收之后，首先判断CA证书是否由可信任的根证书机构发布，如果是则继续判断证书是否被篡改过。
     如果没有，则提取出证书中的公钥，再生成一个对称加密的秘钥。再通过加密算法和公钥加密秘钥再发送给服务器。
4. 服务器通过私钥和解密算法解密出秘钥，并通知浏览器之后的信息传输过程中都通过这个秘钥来加密信息。   